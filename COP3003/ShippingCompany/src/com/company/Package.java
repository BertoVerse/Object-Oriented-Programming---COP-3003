package com.company;
// Package is an interface with the following method below:

import java.util.Scanner;
// The public access specifier indicates that the interface can be used by
// any class in any package
public interface Package {

    double cost(); // determine cost for the package

    void input(Scanner input); // input data for the package

}

